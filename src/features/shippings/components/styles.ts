import {StyleSheet} from "react-native";

export  default   StyleSheet.create({
    container: {
        flex: 1,
    },
    map: {
        width: '100%',
        height: '100%',
    },
    picker: {
        width: 200,
        height:30,
        marginLeft:10,
        marginTop:10,
        marginBottom:10,
        backgroundColor: '#ccc',
        borderRadius: 15,
        overflow: "hidden"
    },
    apply: {
        width: 180,

        marginTop:10,
        marginBottom:10,
        marginRight:10,
        paddingTop: 15,
        marginLeft: 15,
        paddingBottom: 10,
        backgroundColor: '#147ed3',
        borderRadius: 10,
        borderWidth: 1,
        borderColor: '#fff',
    }
});
