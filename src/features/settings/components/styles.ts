import {StyleSheet} from "react-native";

export  default  StyleSheet.create({
    container: {
        flex: 1,
    },
    picker: {
        width: 200,
        height:30,
        backgroundColor: '#ccc',
        borderRadius: 15,
        overflow: "hidden"
    },
});
